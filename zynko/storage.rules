rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /communities/{communityId}/files/{fileName} {
      allow read, write: if request.auth != null;  // Only requires authentication
    }
  }
} 